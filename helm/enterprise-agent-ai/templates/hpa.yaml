{{- if .Values.agentApi.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Release.Name }}-agent-api-hpa
  labels:
    app.kubernetes.io/name: agent-api
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-agent-api
  minReplicas: {{ .Values.agentApi.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.agentApi.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.agentApi.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
